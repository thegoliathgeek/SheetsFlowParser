export default class BlockBuilderHelper {
    static identifyMessageTypeAndBuild(messageType: string, tempHolder: any) {
        switch (messageType) {
            case 'Text':
                break;
        }
    }
}